import React, {FunctionComponent as FC} from 'react';

const Logo: FC = () => (
    <div className="Logo">
        <span className="Logo-initials">rb</span>
        <span className="Logo-remaining">ondoc</span>
    </div>
);

export default Logo;
